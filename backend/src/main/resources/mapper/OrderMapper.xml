<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.secondhand.mapper.OrderMapper">

    <select id="selectOrderPage" resultType="com.campus.secondhand.vo.OrderVO">
        SELECT o.id, o.order_no, o.product_id, p.title AS product_title,
               JSON_UNQUOTE(JSON_EXTRACT(p.images, '$[0]')) AS product_image,
               o.buyer_id, o.buyer_name, o.buyer_phone, o.buyer_address,
               bu.nickname AS buyer_nickname,
               o.seller_id, su.nickname AS seller_name,
               o.price, o.status, o.remark, o.expire_time,
               o.refund_status, o.refund_reason, o.refund_time,
               o.create_time, o.update_time
        FROM t_order o
        LEFT JOIN t_product p ON o.product_id = p.id
        LEFT JOIN t_user bu ON o.buyer_id = bu.id
        LEFT JOIN t_user su ON o.seller_id = su.id
        WHERE o.deleted = 0
        <if test="role != 'ADMIN'">
            AND (o.buyer_id = #{userId} OR o.seller_id = #{userId})
        </if>
        <!-- 非管理员：退款进行中的订单归入“退款/售后”页签，不再出现在状态页签 -->
        <if test="role != 'ADMIN' and (status == null or status != 'REFUND')">
            AND (o.refund_status IS NULL OR o.refund_status NOT IN ('REQUESTED','ARBITRATION'))
        </if>
        <if test="status != null and status == 'REFUND'">
            AND o.refund_status IN ('REQUESTED','ARBITRATION')
        </if>
        <if test="status != null and status != '' and status != 'REFUND'">
            AND o.status = #{status}
        </if>
        <if test="refundStatus != null and refundStatus != ''">
            AND o.refund_status = #{refundStatus}
        </if>
        ORDER BY o.create_time DESC
    </select>

    <select id="selectOrderDetail" resultType="com.campus.secondhand.vo.OrderVO">
        SELECT o.id, o.order_no, o.product_id, p.title AS product_title,
               JSON_UNQUOTE(JSON_EXTRACT(p.images, '$[0]')) AS product_image,
               o.buyer_id, o.buyer_name, o.buyer_phone, o.buyer_address,
               bu.nickname AS buyer_nickname,
               o.seller_id, su.nickname AS seller_name,
               o.price, o.status, o.remark, o.expire_time,
               o.refund_status, o.refund_reason, o.refund_time,
               o.create_time, o.update_time
        FROM t_order o
        LEFT JOIN t_product p ON o.product_id = p.id
        LEFT JOIN t_user bu ON o.buyer_id = bu.id
        LEFT JOIN t_user su ON o.seller_id = su.id
        WHERE o.id = #{id} AND o.deleted = 0
    </select>

    <!-- 销售明细统计：已完成订单，按时间范围过滤 -->
    <select id="selectSalesRows" resultType="com.campus.secondhand.vo.SalesRowVO">
        SELECT o.update_time AS dealTime,
               o.order_no AS orderNo,
               p.title AS productTitle,
               c.name AS categoryName,
               su.nickname AS sellerNickname,
               bu.nickname AS buyerNickname,
               o.price AS price
        FROM t_order o
        JOIN t_product p ON o.product_id = p.id
        LEFT JOIN t_category c ON p.category_id = c.id
        JOIN t_user su ON o.seller_id = su.id
        JOIN t_user bu ON o.buyer_id = bu.id
        WHERE o.deleted = 0
          AND o.status = 'COMPLETED'
          <if test="startTime != null"> AND o.update_time &gt;= #{startTime} </if>
          <if test="endTime != null"> AND o.update_time &lt;= #{endTime} </if>
        ORDER BY o.update_time DESC
    </select>
</mapper>
