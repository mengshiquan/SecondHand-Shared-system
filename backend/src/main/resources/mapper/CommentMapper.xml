<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.secondhand.mapper.CommentMapper">

    <select id="selectCommentPage" resultType="com.campus.secondhand.vo.CommentVO">
        SELECT c.id, c.product_id, c.user_id, u.nickname, u.avatar,
               c.content, c.rating, c.create_time
        FROM t_comment c
        LEFT JOIN t_user u ON c.user_id = u.id
        WHERE c.product_id = #{productId} AND c.deleted = 0
        ORDER BY c.create_time DESC
    </select>
</mapper>
